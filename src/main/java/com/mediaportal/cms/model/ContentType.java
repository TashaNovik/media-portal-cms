package com.mediaportal.cms.model;

/**
 * Enum representing different types of content in the media portal.
 * Used for analytics and recommendations in Redis.
 */
public enum ContentType {
    ARTICLE,
    VIDEO,
    PODCAST
}
